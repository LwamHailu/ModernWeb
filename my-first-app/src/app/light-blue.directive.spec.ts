import { LightBlueDirective } from './light-blue.directive';

describe('LightBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new LightBlueDirective();
    expect(directive).toBeTruthy();
  });
});
